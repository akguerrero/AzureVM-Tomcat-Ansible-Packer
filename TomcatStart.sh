#!/bin/bash

# This test will only show whether Tomcat is able to start or not.
# This script won't take effect on the created VM.
# In order to start Tomcat you have to run this command in the command line.
# Also you're able to configure the Packer image in order to have an automatic Tomcat Start.

sudo bash /usr/local/apache-tomcat-9.0.62/bin/startup.sh


